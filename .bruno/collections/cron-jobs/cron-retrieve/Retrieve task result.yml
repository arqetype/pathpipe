info:
  name: Retrieve task result
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:4100/tasks/:uuid
  params:
    - name: uuid
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
