info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:4000/applications
  body:
    type: json
    data: |-
      {
        "company": "Vinted",
        "position": "SWE"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
